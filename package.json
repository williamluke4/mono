{
  "name": "mono",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "dev": "pnpm run dev --parallel -r --filter=!mono",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "ts-node": "9.0.0",
    "typescript": "4.1.2"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
