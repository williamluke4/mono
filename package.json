{
  "name": "mono",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "wsrun -c install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "wsrun -c build",
    "dev": "wsrun -c dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "ts-node": "9.0.0",
    "typescript": "4.1.2",
    "wsrun": "^5.2.4"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@sindresorhus/tsconfig": "^0.8.0",
    "concurrently": "^5.3.0"
  }
}
